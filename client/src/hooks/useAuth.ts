/**
 * useAuth hook - Custom hook for accessing authentication context
 * This is a convenience re-export of the useAuth hook from AuthContext
 */

export { useAuth, type AuthContextValue, type User, type LoginCredentials, type RegisterCredentials } from '../context/AuthContext';
